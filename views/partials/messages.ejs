<script>
  $error = "<%= typeof error != 'undefined' ? error : null %>";
  if ($error) {
    alert($error);
  }
  $success = "<%= typeof success != 'undefined' ? success : null %>";
  if ($success) {
    alert($success);
  }
</script>
